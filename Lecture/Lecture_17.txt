
npm - node package manager
  레지스트리 라이브러리를 담고있는 역할
  빌드할 때도 사용

npx - 

1. 원래는
  npm install -g create-react-app
  으로 글로벌 디렉토리에 받아야 했음

2. 그러나 npx를 이용하여 다운받지 않고 사용할 수 있음

3. 디스크 낭비가 없고, 항상 최신 버전을 사용 가능하다.